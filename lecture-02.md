# 2강. 등식의 치환과 단계별 계산 증명

> **예상 시간:** 60분 · **선수 학습:** 1강의 가정·목표, `exact`, `intro`
>
> 오늘의 질문은 “같은 값이라는 증거를 이용해 식을 어떻게 바꾸는가?”입니다. 완성 목표는 `rw`로 식을 치환하고, `calc`로 계산의 근거를 한 줄씩 적는 것입니다.

## 1. 1강에서 이어가기

1강에서는 `hPQ : P → Q`에 P의 증거를 넣어 Q의 증거를 만들었습니다. 이번에는 `h : a = b`라는 **등식의 증거**를 사용합니다. a와 b가 같다는 증거가 있으면, 수학적 식에서 a를 b로 바꾸어 추론할 수 있습니다.

`h : a = b`는 변수 a에 b를 대입하는 프로그램 명령이 아닙니다. 이미 주어진 두 대상이 같다는 명제의 증거입니다. Python의 대입이나 상태 변경과 구분하세요.

[1강](./)에서 `intro hP`와 `fun hP => ...`가 새 이름을 도입했던 것처럼, 이번 강의의 `have hbc : ... := ...`도 새 증거에 이름을 붙입니다.

## 2. 준비와 학습 목표

기존 Lean 프로젝트 안에 `Lecture02.lean` 파일을 만드세요. 1강과 마찬가지로 오늘 예제는 **Mathlib import 없이** 실행할 수 있습니다. 숫자는 `Nat`을 사용합니다. 실수와 선형대수로 확장하기 전에 치환 원리 자체를 익히기 위한 선택입니다.

* `rw [h]`와 `rw [← h]`의 방향을 설명한다.
* 목표를 바꾸는 것과 가정을 바꾸는 것을 구분한다.
* `have`로 중간 사실을 만들고 `calc`로 연결한다.
* 각 치환 전에 어떤 식으로 바뀔지 예상한다.

| 시간      | 학습                   |
| ------- | -------------------- |
| 0\~10분  | 등식의 증거와 첫 치환         |
| 10\~25분 | 역방향 치환과 가정의 치환       |
| 25\~40분 | have, calc, 기존 정리 사용 |
| 40\~55분 | 기본 문제 4개             |
| 55\~60분 | 자기 설명과 기록            |

## 3. 첫 치환: rw \[h]

```lean
example (a b : Nat) (h : a = b) : a + 3 = b + 3 := by
  rw [h]
```

처음 가진 것은 `h : a = b`, 목표는 `a + 3 = b + 3`입니다. `rw`는 rewrite의 줄임말로, 등식을 근거로 표현식을 다시 씁니다. `rw [h]`는 h의 **왼쪽 a를 오른쪽 b로** 치환합니다.

```
목표: a + 3 = b + 3
치환: b + 3 = b + 3
결과: 남은 목표 없음
```

이 예제에서는 치환 후 양쪽이 같아져 rw가 마무리합니다. 그래서 뒤에 `rfl`을 또 쓰면 이미 목표가 없다는 오류가 날 수 있습니다. 항상 Infoview에서 남은 목표를 확인하세요.

대괄호 안에는 사용할 등식 증거를 넣습니다. 여러 개라면 `rw [h1, h2]`처럼 순서대로 나열할 수 있습니다.

### exact와 rw의 차이

`exact h`는 h 자체를 최종 증거로 제출합니다. h의 타입은 `a = b`이므로, 목표 `a + 3 = b + 3`과는 다릅니다. 이때는 등식을 식 안에서 이용하는 `rw [h]`가 적절합니다.

| 명령        | 하는 일                        |
| --------- | --------------------------- |
| `exact h` | 현재 목표와 맞는 증거 h를 제출          |
| `rw [h]`  | h가 말하는 등식을 사용해 현재 목표의 식을 치환 |

작은 실험: 위 코드에서 `rw [h]`를 `exact h`로 바꾸고, 기대한 타입과 실제 타입의 차이를 읽은 뒤 되돌려보세요.

## 4. 역방향 치환: rw \[← h]

등식이 원하는 방향과 반대로 주어질 수 있습니다.

```lean
example (a b c : Nat) (h : b = a) (ha : a = c) : b = c := by
  rw [← h] at ha
  exact ha
```

ha 안에는 a가 있고, 이것을 b로 바꾸고 싶습니다. h는 `b = a`이므로 **오른쪽 a에서 왼쪽 b로** 사용해야 합니다.

`←`는 `\l` 뒤 공백으로 입력하거나 복사할 수 있습니다.

| h가 b = a일 때 | 치환 방향 |
| ----------- | ----- |
| `rw [h]`    | b를 a로 |
| `rw [← h]`  | a를 b로 |

화살표는 **h라는 등식의 사용 방향**을 뜻합니다. “목표의 오른쪽만 고쳐라” 또는 “목표의 왼쪽만 고쳐라”라는 뜻이 아닙니다.

이 예제의 `at ha`는 치환할 위치를 ha로 지정합니다. 다음 절에서 따로 살펴봅니다.

## 5. 목표가 아니라 가정을 바꾸기

```lean
example (a b c : Nat) (hab : a = b) (ha : a = c) : b = c := by
  rw [hab] at ha
  exact ha
```

`rw [hab]`만 쓰면 목표에서 치환하려 합니다. 그러나 목표 `b = c`에는 바꿀 a가 없습니다. 이때 필요한 것은 가정 ha의 변경입니다.

```
치환 전:
hab : a = b
ha  : a = c
⊢ b = c

rw [hab] at ha 이후:
hab : a = b
ha  : b = c
⊢ b = c
```

이제 ha가 목표와 정확히 맞으므로 `exact ha`로 끝냅니다. 단순한 이 예제에서는 목표는 그대로이고 ha가 표현하는 명제만 바뀝니다.

### 원래 가정을 보존하고 싶다면 have

```lean
example (a b c : Nat) (hab : a = b) (ha : a = c) : b = c := by
  have hbc : b = c := by
    rw [← hab]
    exact ha
  exact hbc
```

`have hbc : b = c := by ...`는 다음 뜻입니다.

1. b = c라는 중간 사실을 증명한다.
2. 그 증거에 hbc라는 이름을 붙인다.
3. 이후 증명에서 hbc를 사용한다.

hbc는 이전에 선언할 필요가 없습니다. **have가 증거를 만들어 이름을 도입**합니다. `intro`는 목표의 입력을 가정으로 받지만, `have`는 명시한 중간 사실의 증명을 요구한다는 차이가 있습니다.

들여쓰기된 내부 블록에서는 목표가 `b = c`입니다. `rw [← hab]`로 b를 a로 바꾸면 `a = c`가 되어 ha를 제출할 수 있습니다. 원래 ha는 바뀌지 않습니다.

## 6. 종이 계산처럼 쓰는 calc

종이에 다음처럼 쓰는 것은 이미 익숙할 것입니다.

```
a + 3 = b + 3 = c + 3
```

Lean에서는 각 등호가 성립하는 이유까지 적습니다.

```lean
example (a b c : Nat) (hab : a = b) (hbc : b = c) :
    a + 3 = c + 3 := by
  calc
    a + 3 = b + 3 := by rw [hab]
    _ = c + 3 := by rw [hbc]
```

첫 줄은 hab로, 둘째 줄은 hbc로 정당화합니다. 각 `:= by` 뒤는 **그 줄의 등식을 증명하는 작은 증명**입니다.

두 번째 줄의 `_`는 Lean이 문맥에서 추론할 자리이며, 여기서는 이전 줄의 오른쪽인 `b + 3`을 가리킵니다. `sorry`처럼 증명을 생략하는 표시가 아닙니다. `_` 자리에 `b + 3`을 직접 적어도 됩니다.

같은 정리를 짧게 쓰면 다음과 같습니다.

```lean
example (a b c : Nat) (hab : a = b) (hbc : b = c) :
    a + 3 = c + 3 := by
  rw [hab, hbc]
```

짧은 치환은 rw 목록으로, 중간 식을 보여주고 싶은 계산은 calc로 작성해보세요. calc는 계산을 자동으로 해주는 명령이 아니라 **각 단계와 근거를 연결하는 문법**입니다.

## 7. 이미 증명된 정리도 치환 규칙으로 사용하기

가정뿐 아니라 Lean에 있는 정리도 사용할 수 있습니다.

```lean
#check Nat.add_comm
#check Nat.add_assoc
```

핵심 명제는 다음과 같습니다.

| 정리                    | 핵심 내용                       |
| --------------------- | --------------------------- |
| `Nat.add_comm a b`    | `a + b = b + a`             |
| `Nat.add_assoc a b c` | `(a + b) + c = a + (b + c)` |

1강에서 `hPQ hP`로 함수를 적용했듯, 여기서는 정리에 a, b, c를 적용하여 **특정 등식의 증거**를 얻습니다. `Nat.`은 자연수 관련 이름을 모아 놓은 네임스페이스입니다.

```lean
example (a b c : Nat) : (a + b) + c = b + (a + c) := by
  calc
    (a + b) + c = (b + a) + c := by rw [Nat.add_comm a b]
    _ = b + (a + c) := by rw [Nat.add_assoc b a c]
```

먼저 a와 b의 순서를 바꾸고, 다음으로 괄호를 바꿉니다. Lean에서 `a + b + c`는 `(a + b) + c`로 읽습니다.

처음에는 `rw [Nat.add_comm]`처럼 인수를 모두 생략하기보다, `Nat.add_comm a b`처럼 어떤 부분을 바꾸려는지 분명히 적으세요. 여러 곳이 일치하는 식에서는 실제 치환 범위도 Infoview로 확인해야 합니다.

## 8. 직접 풀어보기

아래 sorry를 실제 증명으로 교체하세요. 각 치환 전에 예상하는 목표를 먼저 적습니다. 처음 네 문제만으로 기본 학습을 완료할 수 있습니다.

### 문제 1 — 함수 안에서 치환하기

```lean
example (a b : Nat) (f : Nat → Nat) (h : a = b) :
    f a = f b := by
  sorry
```

f가 어떤 함수인지 모르더라도 같은 입력의 함수값은 같아야 합니다.

### 문제 2 — 방향을 선택하기

```lean
example (a b c : Nat) (hab : a = b) (hac : a = c) :
    b + 2 = c + 2 := by
  sorry
```

목표의 b를 a로 바꾼 다음, a를 c로 바꾸어보세요.

### 문제 3 — 가정의 함수값을 바꾸기

```lean
example (a b c : Nat) (f : Nat → Nat)
    (hab : a = b) (ha : f a = c) : f b = c := by
  sorry
```

ha에 치환을 적용하고 exact로 끝내세요. 여유가 있으면 have를 사용해 ha를 보존하는 풀이도 작성하세요.

### 문제 4 — calc 완성하기

```lean
example (a b c : Nat) : a + (b + c) = b + (a + c) := by
  calc
    a + (b + c) = (a + b) + c := by sorry
    _ = (b + a) + c := by sorry
    _ = b + (a + c) := by sorry
```

세 줄에 각각 어떤 정리가 어느 방향으로 필요한지 적고 증명하세요.

### 선택 도전 — 1강의 intro와 연결하기

```lean
example (a b : Nat) (f : Nat → Nat) :
    a = b → f a = f b := by
  sorry
```

문제 1의 h가 처음에는 없습니다. 어디에서 도입해야 할까요?

[2강 힌트와 해설](lecture-02-solutions.md)은 별도 페이지에 있습니다. 답을 보기 전에 최소한 다음 목표가 무엇인지 예상해보세요.

## 9. 오류를 해석하는 순서

| 증상                    | 확인과 해결                                       |
| --------------------- | -------------------------------------------- |
| rw가 일치하는 식을 찾지 못함     | 규칙의 왼쪽이 목표에 있는지 확인. 방향이 반대면 ←, 가정에 있으면 at 사용 |
| 치환이 예상과 다름            | 현재 식의 괄호와 인수 확인. 사용할 정리의 인수를 명시              |
| no goals to be solved | 앞 명령이 이미 증명을 끝냈는지 확인. 불필요한 마지막 명령 제거         |
| calc가 이어지지 않음         | 이전 줄의 오른쪽과 다음 줄의 왼쪽이 이어지는지 확인                |
| hbc를 찾을 수 없음          | have를 도입한 위치와 들여쓰기 범위 확인                     |
| rfl만으로 끝나지 않음         | 정의에 따른 계산만으로 같은지, 교환·결합 법칙 같은 정리가 필요한지 구분    |

`rw`는 문자열 찾기·바꾸기가 아닙니다. 타입이 있는 수학적 표현식에서 등식의 증거를 이용합니다. 우선 이번 강의의 단순한 식에서 방향과 위치를 정확히 읽는 데 집중하세요.

## 10. 완료 체크와 내 기록

* [ ] `h : a = b`가 대입 명령과 다른 이유를 설명할 수 있다.
* [ ] `rw [← h]`에서 화살표가 무엇의 방향인지 안다.
* [ ] `rw [h] at ha` 전후의 ha와 목표를 설명할 수 있다.
* [ ] `have`로 도입한 이름이 어디서 생겼는지 설명할 수 있다.
* [ ] 기본 문제 4개를 sorry 없이 풀었다.

| 항목                     | 내 기록 |
| ---------------------- | ---- |
| 날짜와 소요 시간              |      |
| 방향을 잘못 선택했던 치환         |      |
| 치환 전 예상한 목표 / 실제 목표    |      |
| exact와 rw의 차이를 내 말로 설명 |      |
| 다시 풀 문제                |      |

다음 학습을 시작할 때 문제 2와 3을 답 없이 다시 풀어보세요. 다음 강의에서는 “그리고”, “또는”이 들어간 명제의 증거를 만들고 사용하는 법을 배웁니다.

## 참고 자료

* [Mathematics in Lean 2장](https://leanprover-community.github.io/mathematics_in_lean/C02_Basics.html): 계산, 치환, calc의 수학적 사용.
* [Theorem Proving in Lean 4: Tactics](https://lean-lang.org/theorem_proving_in_lean4/Tactics/): rw, have 등 전술의 원리.

설명과 문제는 이 학습 과정에 맞춰 새로 구성했습니다.

예제 확인: 본문과 해설의 완성 Lean 코드 블록은 Lean 4.33.1에서 오류·경고 없이 검사했습니다. 연습문제의 sorry 틀은 완성 증명 검사에서 제외했습니다.
