# 2강 힌트와 해설

[2강 본문](lecture-02.md)의 문제를 먼저 풀어보세요. 이 페이지는 **힌트 → 완성 풀이 → 목표 변화 설명** 순서로 사용합니다.

## 힌트만 보기

| 문제    | 첫 힌트                                        |
| ----- | ------------------------------------------- |
| 1     | h로 a를 b로 치환하면 f a는 무엇이 될까요?                 |
| 2     | hab는 a에서 b 방향입니다. 지금은 b에서 a로 가야 합니다.        |
| 3     | 바꿀 a가 들어 있는 곳은 목표가 아니라 ha입니다.               |
| 4     | 첫 줄은 결합 법칙의 역방향, 둘째는 교환, 셋째는 결합 법칙의 정방향입니다. |
| 선택 도전 | 목표가 함의이므로 먼저 intro로 등식의 증거를 받으세요.           |

여기까지만 읽고 본문으로 돌아가 다시 시도하세요.

## 문제 1 — 같은 입력의 함수값

```lean
example (a b : Nat) (f : Nat → Nat) (h : a = b) :
    f a = f b := by
  rw [h]
```

목표의 f a를 f b로 바꾸면 양쪽이 같아집니다. f의 구체적인 계산 방법을 알 필요는 없습니다. 같은 대상을 함수에 넣는다는 사실만 사용합니다.

1강처럼 증거를 직접 제출하는 다른 풀이도 가능합니다.

```lean
example (a b : Nat) (f : Nat → Nat) (h : a = b) :
    f a = f b := by
  exact congrArg f h
```

congrArg는 등식의 양쪽에 같은 함수를 적용한 등식을 만드는 정리입니다. f와 h를 받아 f a = f b의 증거를 줍니다. 지금은 rw 풀이를 먼저 익히면 충분합니다.

## 문제 2 — 역방향 치환

```lean
example (a b c : Nat) (hab : a = b) (hac : a = c) :
    b + 2 = c + 2 := by
  rw [← hab]
  rw [hac]
```

| 단계          | 목표               |
| ----------- | ---------------- |
| 시작          | b + 2 = c + 2    |
| rw \[← hab] | a + 2 = c + 2    |
| rw \[hac]   | 양쪽이 c + 2가 되어 종료 |

첫 단계의 화살표는 hab의 오른쪽 b에서 왼쪽 a로 치환한다는 뜻입니다. 목표의 어느 변을 고를지 지시하는 기호가 아닙니다.

두 줄을 합쳐 `rw [← hab, hac]`로 작성해도 됩니다. 명령을 외우기보다 치환 순서를 설명할 수 있는지 확인하세요.

## 문제 3 — 가정 안에서 치환

```lean
example (a b c : Nat) (f : Nat → Nat)
    (hab : a = b) (ha : f a = c) : f b = c := by
  rw [hab] at ha
  exact ha
```

처음 ha는 f a = c입니다. hab를 적용하면 ha가 f b = c라는 증거가 되어 목표와 맞습니다. `at ha`를 생략하면 이 경우 목표에서 a를 찾을 수 없어 의도대로 작동하지 않습니다.

### 원래 가정을 보존하는 풀이

```lean
example (a b c : Nat) (f : Nat → Nat)
    (hab : a = b) (ha : f a = c) : f b = c := by
  have hb : f b = c := by
    rw [← hab]
    exact ha
  exact hb
```

hb는 have 줄에서 새로 도입합니다. 타입 f b = c를 써준 것만으로 가정이 생기는 것은 아닙니다. 내부 블록에서 그 명제를 증명해야 합니다.

내부 목표의 b를 a로 바꾸면 f a = c이고, 이것의 증거는 ha입니다. 블록을 마치면 hb를 바깥 증명에서 사용할 수 있습니다.

| 도구                | 새 이름이 생기는 방식            |
| ----------------- | ----------------------- |
| intro h           | 함의 목표의 입력을 가정으로 받음      |
| fun h => ...      | 함수를 정의하며 매개변수 이름을 도입    |
| have h : T := ... | T의 증거를 구성한 뒤 h라는 이름을 붙임 |

이 표는 1강에서 질문했던 “선언하지 않은 이름을 왜 쓸 수 있는가?”와 연결됩니다. 각 문법이 바로 이름을 도입하는 자리입니다.

## 문제 4 — calc의 각 줄을 증명하기

```lean
example (a b c : Nat) : a + (b + c) = b + (a + c) := by
  calc
    a + (b + c) = (a + b) + c := by rw [← Nat.add_assoc a b c]
    _ = (b + a) + c := by rw [Nat.add_comm a b]
    _ = b + (a + c) := by rw [Nat.add_assoc b a c]
```

첫 단계에서 필요한 등식은 오른쪽 결합을 왼쪽 결합으로 바꾸는 것입니다. Nat.add\_assoc는 (a + b) + c = a + (b + c)이므로 역방향으로 사용합니다.

두 번째 단계는 a와 b를 교환합니다. 세 번째는 b, a, c에 결합 법칙을 정방향으로 적용합니다.

각 줄은 전체 원래 목표를 다시 증명하는 것이 아닙니다. **그 줄에 적힌 양쪽 식의 등식만** 증명합니다. Lean이 등식의 추이성을 이용해 그 단계들을 이어줍니다.

### 같은 구조를 exact로 쓰기

```lean
example (a b c : Nat) : a + (b + c) = b + (a + c) := by
  calc
    a + (b + c) = (a + b) + c := (Nat.add_assoc a b c).symm
    _ = (b + a) + c := by rw [Nat.add_comm a b]
    _ = b + (a + c) := Nat.add_assoc b a c
```

.symm은 등식 증거의 방향을 뒤집습니다. 첫째와 셋째 줄은 그 줄의 목표와 정확히 맞는 증거를 직접 넣었으므로 := 뒤에 by가 필요하지 않습니다. 가운데 줄은 큰 식 안의 일부를 치환하므로 rw를 썼습니다.

1강의 `:= fun hP => ...`와 마찬가지로, 증거를 직접 적는 자리와 전술로 증거를 만드는 `by` 블록을 구분해보세요.

## 선택 도전 — intro 다음에 rw

```lean
example (a b : Nat) (f : Nat → Nat) :
    a = b → f a = f b := by
  intro h
  rw [h]
```

intro h가 h : a = b를 새로 도입합니다. 이후는 문제 1과 같습니다. h라는 이름은 다른 이름으로 바꿀 수 있습니다.

함수 형태로 쓰면 다음과 같습니다.

```lean
example (a b : Nat) (f : Nat → Nat) :
    a = b → f a = f b :=
  fun h => congrArg f h
```

이 함수는 등식 a = b의 증거 h를 받아 등식 f a = f b의 증거를 반환합니다.

## 이해 확인과 자기 평가

1. `rw [← hab]`는 목표의 오른쪽만 바꾸라는 뜻인가요?
2. calc의 `_`는 미완성 증명인가요?
3. `have hb : f b = c := by ...`에서 hb를 미리 선언해야 하나요?
4. rw 다음 rfl을 썼는데 “목표가 없다”는 오류가 나면 어떻게 하나요?

답: 1번은 아닙니다. hab의 사용 방향입니다. 2번도 아닙니다. 문맥에서 추론되는 표현식 자리입니다. 3번은 have가 이름을 도입하므로 필요 없습니다. 4번은 앞의 rw가 이미 마무리했는지 확인하고 불필요한 rfl을 제거합니다.

| 수준       | 기준                                  |
| -------- | ----------------------------------- |
| 복습 필요    | 코드가 통과하지만 어떤 식을 바꾸었는지 설명하기 어렵다      |
| 기본 완료    | 기본 문제 4개의 치환 전후를 설명할 수 있다           |
| 다음 강의 준비 | 해설 없이 문제를 다시 풀고 intro와 rw를 연결할 수 있다 |

학습 기록에는 “명령이 떠오르지 않았음”보다 **어떤 등식을 어느 방향으로 어디에 적용하려 했는지** 적어보세요.
