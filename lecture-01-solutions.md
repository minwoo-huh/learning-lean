# 1강 힌트와 해설

먼저 1강 본문의 문제를 직접 풀어보세요. 해설을 본 문제는 다음 날 답을 닫고 다시 풀어야 학습한 것으로 셉니다.

## 힌트만 보기

| 문제 | 첫 번째 힌트                                                                         |
| -- | ------------------------------------------------------------------------------- |
| 1  | 목표가 Q입니다. P의 증거와 Q의 증거 중 어느 것이 필요한가요?                                           |
| 2  | R을 만들 수 있는 함수는 hQR입니다. 거꾸로 R → Q → P 순서로 목표를 줄여보세요. 이 화살표는 여기서 목표 변화 순서를 나타냅니다. |
| 3  | 목표가 P → R입니다. 먼저 P의 증거를 입력으로 받은 다음 문제 2의 추론을 사용하세요.                             |
| 4  | shiftByThree의 정의를 펼쳐 계산하면 4 + 3입니다. 정의에 따른 계산으로 같아지는 등식에 썼던 전술을 떠올리세요.          |

이제 본문으로 돌아가 한 번 더 시도하세요. 아래에는 완성 풀이가 있습니다.

## 문제 1 — 목표와 타입을 맞추기

```lean
example (P Q : Prop) (hP : P) (hQ : Q) : Q := by
  exact hQ
```

hP가 아니라 hQ를 제출합니다. 가정의 이름보다 콜론 뒤의 타입이 중요합니다. hP를 사용하지 않았다는 경고가 표시되더라도 그것은 증명 실패와 다릅니다.

## 문제 2 — 뒤에서부터 연결하기

```lean
example (P Q R : Prop)
    (hP : P) (hPQ : P → Q) (hQR : Q → R) : R := by
  apply hQR
  apply hPQ
  exact hP
```

목표는 R에서 Q, 그다음 P로 바뀝니다. 이미 hP가 있으므로 마지막 의무를 해결할 수 있습니다.

한 줄로 쓰면 다음과 같습니다.

```lean
example (P Q R : Prop)
    (hP : P) (hPQ : P → Q) (hQR : Q → R) : R := by
  exact hQR (hPQ hP)
```

안쪽 hPQ hP의 타입은 Q이고, 여기에 hQR을 적용한 결과의 타입은 R입니다. 괄호는 함수 적용의 순서를 명확히 합니다.

## 문제 3 — 가정을 도입한 뒤 연결하기

```lean
example (P Q R : Prop)
    (hPQ : P → Q) (hQR : Q → R) : P → R := by
  intro hP
  apply hQR
  apply hPQ
  exact hP
```

intro 이전에는 P의 증거가 없습니다. intro hP 이후 hP : P가 가정에 추가되고 목표는 R이 됩니다.

프로그래밍에 익숙하다면 다음 형태도 읽어보세요. 두 함수를 합성해 새 함수를 만드는 모습입니다.

```lean
example (P Q R : Prop)
    (hPQ : P → Q) (hQR : Q → R) : P → R :=
  fun hP => hQR (hPQ hP)
```

두 방식 중 처음에는 단계마다 목표를 볼 수 있는 전술 방식으로 연습하세요.

### hP는 어디에서 선언되었을까?

처음에는 `hP`가 없습니다. **`intro hP`가 새 가정을 도입하고 이름을 붙이는 줄**입니다. 기존 변수를 찾아 사용하는 명령이 아닙니다.

처음 증명 상태는 다음과 같습니다.

```
P Q R : Prop
hPQ : P → Q
hQR : Q → R
⊢ P → R
```

목표 `P → R`은 “P의 증거를 받으면 R의 증거를 만들겠다”는 뜻입니다. 종이 증명에서 “P라고 가정하고 R을 증명하자”라고 시작하는 것을 Lean에서는 `intro hP`로 표현합니다.

```
intro hP 이후:

P Q R : Prop
hPQ : P → Q
hQR : Q → R
hP : P
⊢ R
```

타입을 따로 쓰지 않아도 되는 이유는 목표 `P → R`의 입력 부분이 P이므로 Lean이 새 가정의 타입을 알기 때문입니다. `hP`라는 이름은 자유롭게 정할 수 있습니다.

```lean
example (P Q R : Prop)
    (hPQ : P → Q) (hQR : Q → R) : P → R := by
  intro evidenceOfP
  apply hQR
  apply hPQ
  exact evidenceOfP
```

이 이름은 해당 증명 안에서 사용합니다. 다른 `example`에도 자동으로 생기는 전역 변수가 아닙니다.

| 명령          | 의미                        | 남은 목표 |
| ----------- | ------------------------- | ----- |
| `intro hP`  | P의 증거를 입력으로 받아 hP라고 이름 붙임 | `R`   |
| `apply hQR` | Q의 증거가 있으면 R의 증거를 만들 수 있음 | `Q`   |
| `apply hPQ` | P의 증거가 있으면 Q의 증거를 만들 수 있음 | `P`   |
| `exact hP`  | 처음 입력받은 P의 증거를 제출함        | 없음    |

P가 무조건 참이라고 주장한 것은 아닙니다. **P의 증거가 주어졌을 때 R의 증거를 만드는 방법**을 증명했습니다.

### 함수 형태의 증명을 한 단계씩 읽기

```lean
example (P Q R : Prop)
    (hPQ : P → Q) (hQR : Q → R) : P → R :=
  fun hP => hQR (hPQ hP)
```

여기서는 전술 대신, 목표 타입 `P → R`을 갖는 함수를 직접 작성합니다.

**1. `fun hP =>`: 입력을 받고 이름 붙이기**

`fun 입력이름 => 반환할_표현식`은 이름 없는 함수를 만드는 문법입니다. 이 위치에 필요한 함수의 타입이 `P → R`이므로, Lean은 입력 `hP`의 타입을 P로 추론합니다. 명시적으로 쓰면 다음과 같습니다.

```lean
example (P Q R : Prop)
    (hPQ : P → Q) (hQR : Q → R) : P → R :=
  fun (hP : P) => hQR (hPQ hP)
```

여기에서도 hP는 미리 존재한 변수가 아닙니다. `fun`이 매개변수로 도입하며, `=>` 오른쪽의 함수 본문에서 사용할 수 있습니다. `intro hP`와 같은 입력 도입을 함수 문법으로 표현한 것입니다.

**2. `hPQ hP`: P의 증거로 Q의 증거 만들기**

Lean의 `f x`는 함수 f에 x를 적용한다는 뜻입니다. `hPQ : P → Q`에 `hP : P`를 넣으므로 `hPQ hP`의 타입은 Q입니다.

**3. `hQR (hPQ hP)`: Q의 증거로 R의 증거 만들기**

안쪽에서 얻은 Q의 증거를 `hQR : Q → R`에 넣습니다. 전체 표현식의 타입은 R입니다.

| 표현                       | 타입      | 역할                       |
| ------------------------ | ------- | ------------------------ |
| `hP`                     | `P`     | 함수가 입력받은 증거              |
| `hPQ hP`                 | `Q`     | 첫 번째 함수를 적용한 결과          |
| `hQR (hPQ hP)`           | `R`     | 두 번째 함수를 적용한 결과          |
| `fun hP => hQR (hPQ hP)` | `P → R` | P의 증거에서 R의 증거를 만드는 전체 함수 |

```
hP : P
  │ hPQ 적용
  ▼
hPQ hP : Q
  │ hQR 적용
  ▼
hQR (hPQ hP) : R
```

괄호는 `hPQ hP` 전체를 hQR의 입력으로 묶습니다. 괄호를 지운 `hQR hPQ hP`는 같은 의미가 아닙니다. Lean의 함수 적용은 왼쪽부터 묶이므로 `(hQR hPQ) hP`로 읽히며, 이 예제에서는 hQR이 요구하는 Q의 증거 자리에 함수 hPQ를 넣어 타입이 맞지 않게 됩니다.

### Python과 함수 합성에 연결하기

Python 문법으로 형태만 대응시키면 다음과 같습니다. 이 코드는 Lean 증명을 검증하는 Python 코드가 아니라, 함수 구조를 이해하기 위한 비유입니다.

```python
lambda hP: hQR(hPQ(hP))
```

수학의 함수 합성 `hQR ∘ hPQ`와 같은 구조입니다. 먼저 hPQ를 적용하고, 그 결과에 hQR을 적용합니다.

전술 풀이는 목표를 **R에서 Q, 다시 P로** 거슬러 내려가며 필요한 증거를 찾습니다. 함수 풀이는 주어진 증거를 **P에서 Q, 다시 R로** 변환하는 과정을 직접 씁니다. 두 방향 모두 같은 함의의 합성을 표현합니다.

### 이해 확인

* `intro hP`를 `intro evidence`로 바꾸면 마지막 줄은 어떻게 바뀌어야 할까요?
* `fun hP => hPQ hP`만 작성하면 결과 함수의 타입은 무엇일까요?
* 왜 `hQR (hPQ hP)`의 괄호를 생략할 수 없을까요?

답: 마지막 줄은 `exact evidence`가 됩니다. 두 번째 함수의 타입은 `P → Q`이므로 목표 `P → R`을 일반적으로 충족하지 못합니다. 세 번째는 괄호를 생략하면 함수 적용의 묶임이 달라져 입력 타입이 맞지 않기 때문입니다.

## 문제 4 — 정의에 따른 계산

본문에서 shiftByThree를 이미 선언했다면 아래 example만 추가하세요. 같은 파일에서 def를 두 번 선언하면 이름 중복 오류가 납니다.

```lean
example : shiftByThree 4 = 7 := by
  rfl
```

왼쪽은 정의에 따라 4 + 3으로, 다시 7로 계산됩니다. 오른쪽과 정의상 같으므로 rfl로 끝납니다.

## 추가 도전 — 앞으로 연결해보기

문제 2를 중간 증거를 명시하면서 풀 수도 있습니다. have는 다음 강의에서 더 다룹니다.

```lean
example (P Q R : Prop)
    (hP : P) (hPQ : P → Q) (hQR : Q → R) : R := by
  have hQ : Q := hPQ hP
  exact hQR hQ
```

여기서는 목표를 줄이는 대신, 이미 아는 P에서 Q의 증거를 먼저 만듭니다. 같은 논리를 전진·후진 두 방향으로 작성할 수 있다는 점을 확인하세요.

## 스스로 채점하기

정답과 명령 순서가 다르더라도, 올바른 가정에서 같은 결론을 증명하고 sorry 없이 검사를 통과하면 정답입니다.

| 수준          | 판단 기준                                  |
| ----------- | -------------------------------------- |
| 아직 연습 중     | 해설을 복사하면 되지만 각 줄을 설명하기 어렵다.            |
| 기본 완료       | 각 줄의 입력·목표 변화를 설명할 수 있다.               |
| 다음 강의 준비 완료 | 해설 없이 다시 풀고 P, Q, R의 이름을 바꾼 문제에도 대응한다. |

기록할 질문: “나는 어느 문제에서 수학적 추론이 막혔고, 어느 문제에서 Lean 문법이 막혔는가?”
